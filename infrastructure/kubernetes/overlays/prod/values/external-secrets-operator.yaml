# External Secrets Operator - Production Environment
# Vault secret sync için production kaynak limitleri

external-secrets:
  resources:
    requests:
      cpu: 100m        # Production için artırıldı (dev: 25m)
      memory: 256Mi    # Production için artırıldı (dev: 64Mi)
    limits:
      cpu: 500m        # Production için artırıldı (dev: 100m)
      memory: 512Mi    # Production için artırıldı (dev: 128Mi)
