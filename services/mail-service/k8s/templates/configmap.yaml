apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mail-service.fullname" . }}-config
  labels:
    {{- include "mail-service.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
