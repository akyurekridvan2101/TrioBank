{{- if .Values.externalSecrets }}
{{- range .Values.externalSecrets }}
{{- if .enabled }}
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ include "mail-service.fullname" $ }}-{{ .name }}
  namespace: {{ $.Release.Namespace }}
  labels:
    {{- include "mail-service.labels" $ | nindent 4 }}
    triobank.com/secret-type: {{ .name }}
spec:
  refreshInterval: {{ .refreshInterval | default "1h" }}
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: {{ .targetSecret }}
    creationPolicy: Owner
  data:
  {{- $vaultPath := .vaultPath }}
  {{- range .fields }}
  - secretKey: {{ .secretKey }}
    remoteRef:
      key: {{ $vaultPath }}
      property: {{ .property }}
  {{- end }}
---
{{- end }}
{{- end }}
{{- end }}
