
replicaCount: 1

image:
  tag: "dev"
  pullPolicy: Always

app:
  environment: development
  logLevel: debug
  logFormat: text

resources:
  requests:
    memory: 128Mi
    cpu: 50m
  limits:
    memory: 256Mi
    cpu: 200m

databases:
  mssql:
    externalName: host.minikube.internal  # Minikube Linux gateway to host
    portName: mssql

database:
  name: ledger_db

